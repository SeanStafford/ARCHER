# System Dependencies for ARCHER Rendering Context
#
# This file lists Debian/Ubuntu packages required for PDF compilation

# LaTeX binaries - provides pdflatex and xetex engine
texlive-binaries

# XeLaTeX support - provides xelatex command (XeTeX + LaTeX)
texlive-xetex

# NOTICE
# List ONLY package names (as used with apt-get install), not command names
# To find which package provides a command:
#   dpkg -S $(which command-name)
#   OR: apt-file search command-name
#
# Installation (Ubuntu/Debian):
#   sudo apt-get install $(grep -v '^#' dependencies.txt | xargs)
