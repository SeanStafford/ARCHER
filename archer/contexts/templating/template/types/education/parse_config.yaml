# Parsing configuration for education
# Note: This type uses custom parsing logic due to complex nested structure
# Config documents expected patterns for reference

operations:
  # Set type field
  type_field:
    extract_literal: type
    value: education
